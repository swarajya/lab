check current used branch
---------------------------
git branch

change branch
------------
sudo git checkout -b development

Set tracking
----------
sudo git branch --set-upstream-to=origin/development development



or create a new repository on the command line
echo "# amplifydemo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/swarajya/amplifydemo.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/swarajya/amplifydemo.git
git branch -M main
git push -u origin main

